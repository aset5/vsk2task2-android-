<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.task2vsk2">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Task2vsk2"
        tools:targetApi="31">

        <!-- Main launcher -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.AdminActivity" />
        <activity android:name=".activity.TeacherActivity" />
        <activity android:name=".activity.StudentActivity" />
        <activity android:name=".activity.AddCourseActivity" />
        <activity android:name=".activity.AssignUserToCourseActivity" />
        <activity android:name=".activity.AddAssignmentActivity" />
        <activity android:name=".activity.GradeStudentsActivity" />
        <activity android:name=".activity.StudentCourseDetailActivity" />

    </application>

</manifest>
